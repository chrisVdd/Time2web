{% extends '::base.html.twig' %}

{% block content %}
    
    {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}

        <div class="form-group">
            {{ form_row(edit_form.name) }}
        </div>
        <div class="form-group">
            {{ form_row(edit_form.presentation) }}
        </div>
        <div class="form-group">
            {{ form_row(edit_form.web) }}
        </div>
        <div class="form-group">
            {{ form_row(edit_form.contactEmail) }}
        </div>

        <input type="submit" />
    {{ form_end(edit_form) }}
{% endblock content %}
